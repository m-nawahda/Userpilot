import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  cell: {
    display: "flex",
    flexDirection: "row",
  },
  boxInfo: {
    display: "flex",
    flexDirection: "column",
  },
});
