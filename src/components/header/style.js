import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  header: {
    margin: "30px",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
  },
  profileBox: {
    display: "flex",
    alignItems: "center",
    columnGap: "10px",
  },
});
